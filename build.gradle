group 'io.palsson.java8imp'
version '1.0'

apply plugin: 'java'

sourceCompatibility = 1.8

repositories {
    mavenCentral()
}

dependencies {
    compile (
        [group: 'org.immutables', name: 'immutables', version: '2.4.4'],
        [group: 'io.javaslang', name: 'javaslang', version: '2.1.0-alpha'],
        [group: 'org.glassfish.jersey.core', name: 'jersey-client', version: '2.22.2'],
        [group: 'org.glassfish.jersey.ext.rx', name: 'jersey-rx-client-rxjava', version: '2.22.2'],
        [group: 'org.glassfish.jersey.ext.rx', name: 'jersey-rx-client-java8', version: '2.22.2']
    )
    testCompile group: 'org.testng', name: 'testng', version: '6.9.10'
}

task homeDir {
    doLast {
        println gradle.gradleHomeDir
    }

}

task showCache {
    doLast {
        configurations.compile.each { println it }
    }
}
